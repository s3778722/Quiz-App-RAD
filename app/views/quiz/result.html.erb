
<h1 class="text-center">This time, you answered <%= session[:marks]%>/<%= session[:max] %> questions correctly</h1>
<% session[:marks] = 0 %>
<% session[:times] = 0 %>
<% session[:history][0..4].each do |i| %>
  <h5 class="text-center"><%=i%></h5>
<% end %>


<div class="row">
    <div class="col text-center">
      <a class="btn btn-primary" href="/quiz/reload" role="button">Reload</a>
      <p class="text-center">*Reload is based of remember settings*</p>
      <br>
      <a class="btn btn-primary" href="/quiz/redo" role="button">Redo</a>
      <p class="text-center">*Redo is based the same identical questions from previous attempt*</p>
    </div>
</div>
<p class="text-center">*Please wait patiently, while it generates the Quizzes.It may take sometime in Heroku*</p>